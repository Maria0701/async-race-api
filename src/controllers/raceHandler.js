export const raceHandler = () => {
    const raceElts = document.querySelectorAll('[data-name]');

    raceElts.map((item) => {
        btn.disabled = true;


        // const queryObj: StringObj = {
        //     id: item.dataset.name,
        //     status: DETAILED_BTNS,
        // };
    })

    
} 